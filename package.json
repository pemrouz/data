{
  "name": "data",
  "version": "4.0.0",
  "description": "Reactive data engine — sources, a closed delta algebra, incremental operators, native/wire subscriptions, and a DOM render layer, under the executable SCHEDULE timing contract. The substrate fero replicates over.",
  "type": "module",
  "license": "MIT",
  "author": "pemrouz",
  "exports": {
    ".": {
      "types": "./types/public.d.ts",
      "import": "./api/index.ts"
    },
    "./jsx-runtime": {
      "import": "./api/jsx-runtime.ts"
    },
    "./jsx-dev-runtime": {
      "import": "./api/jsx-runtime.ts"
    },
    "./devtools": {
      "import": "./devtools/entry.ts"
    }
  },
  "sideEffects": [
    "./ops/*.ts"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --test api/*.test.ts kernel/*.test.ts ops/*.test.ts seam/*.test.ts conformance/*.test.ts render/*.test.ts jsx/*.test.ts devtools/*.test.ts",
    "typecheck": "tsc -p types && tsc -p types/tsconfig.jsx.json && tsc -p types/tsconfig.auto.json && tsc -p types/tsconfig.public.json",
    "perf": "node --experimental-strip-types --no-warnings perf/m1-gate.ts && node --experimental-strip-types --no-warnings --expose-gc perf/m2-gate.ts && node --experimental-strip-types --no-warnings perf/m3-replication.ts && node --experimental-strip-types --no-warnings --expose-gc perf/m4-budgets.ts",
    "bench:commit": "node --experimental-strip-types --no-warnings perf/commit.bench.ts",
    "test:all": "npm test && npm run typecheck && npm run perf"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
