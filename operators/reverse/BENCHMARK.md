# reverse — benchmark

Workload: 10_000 rows; tick inserts one row at a fresh key (the reversed
output gains one element at the front). Batch streams 1000 inserts.
Generated by [comparisons/bench/operators/reverse.bench.ts](../../comparisons/bench/operators/reverse.bench.ts).

| Library | Setup (ms) | Single (ms) | vs data | Batch 1000 (ms) | vs data |
|---|---:|---:|---:|---:|---:|
| **data** | 1.28 | **0.006** | — | **3.81** | — |
| svelte-store | 0.621 | 0.037 | 6.2× | 140.30 | 36.8× |
| solid | 0.766 | 0.044 | 7.3× | 91.38 | 24.0× |
| preact-signals | 0.780 | 0.045 | 7.5× | 85.18 | 22.4× |
| rxjs | 0.815 | 0.053 | 8.8× | 83.59 | 21.9× |
| mobx | 101.20 | 0.054 | 9.0× | 58.72 | 15.4× |
| react | 0.797 | 0.067 | 11.2× | 165.27 | 43.4× |
| vue-reactivity | 13.72 | 8.12 | 1353× | 11618.93 | 3050× |


data is fastest on both single (2.8x over rxjs) and batch (4x over mobx).

## How

[index.ts](index.ts) maintains a single output array. BI0 (insert at a
fresh key) prepends the new value(s) — incremental, no rebuild. Removes
and updates still rebuild because reverse-mapping a value to its position
is O(N) and ambiguous for duplicate values; that's the right trade-off
for the common workload (streaming inserts).

Run `BENCH_OPS=reverse npm run bench:ops` to refresh — update this file
when the numbers change materially.
