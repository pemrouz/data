/node_modules/
/.claude/scheduled_tasks.lock

# the landing site: lib/, gen/ and index.html are GENERATED by site/strip.mjs + site/build.mjs.
# INTERIM (2026-09-21): they are tracked, because GitHub Pages serves this branch's root directly
# (the built-in branch deploy) while the repo's Actions are locked — regenerate before committing
# (node site/strip.mjs && node site/build.mjs); once .github/workflows/pages.yml deploys again,
# ignore them here, git rm --cached them and drop the root index.html redirect.
# /site/lib/
# /site/gen/
# /site/index.html
/site/shots/
