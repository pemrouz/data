/node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/build/
/experiments/wasm/build/
/.claude/scheduled_tasks.lock
*.js
!**/flights.js
!assets/*.js
!examples/multidim/**/*.js
!examples/flow/**/*.js
!examples/swarm/**/*.js
!examples/kanban/**/*.js
!examples/pivot/**/*.js
!examples/library/**/*.js
!examples/crossfilter-v3/**/*.js
!perf/**/*.js
!mockups/**/*.js

# perf report: the raw per-run sweep output is transient. The collated
# perf/perf.json and perf/history.jsonl ARE committed (the dashboard at /perf/
# fetches the former; the latter is the trend changelog); only run dirs ignored.
/perf/results/

# dist/ is committed so GitHub Pages (branch deploy) can serve the importmap
# targets while CI/Actions billing is unavailable. The Actions workflow in
# .github/workflows/pages.yml is the preferred path once billing is restored —
# at which point this committed dist/ can be dropped again. Track the built
# .js/.d.ts; skip the .map files (not needed to serve, and they bloat history).
# Rebuild + recommit dist/ on each release.
!/dist/**
/dist/**/*.map

# test report (tests/report): commit sources + built explorer.html; ignore the run artifact
!tests/report/**/*.js
tests/report/results.json
