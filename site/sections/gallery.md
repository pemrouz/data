**a · the old grid** (untouched, still numbered 06) — sec-head + lead-para, then the old `.gallery` grammar: a `.gcard-feature` for fero (prose left; the coupling as a mono kv panel right — entry / ride / wire — with the seven names as chips), then three `.gcard`s: the essay ("Write the view, flow the change", read →), the proof ("18 of 18 in this tab" with a live 18-cell run strip that replays the suite, clause names ticking, ▶ again), and the tooling ("See your graph", links #devtools). Foot: "examples/ returns with the v4 gallery — crossfilter, chat, to-do."
**b · fero only** (THE PICK, reworked 2026-09-12) — sec-head **07** (a new 06 "Across the wire" precedes it) + the two-sentence lead (fero: replicated, durable data kernel — deterministic core, real-TCP / real-WAL shells — every write and replica a data source under this contract; its CI rides data's conformance suite against data HEAD). Then ONE wide `.gcard-feature`: tag "consumer · in production", title `fero` (→ github.com/pemrouz/fero), two sentences on what fero is (a deterministic kernel proven both ways from the same bytes — seeded chaos sim + real TCP / WAL; per-cell LWW replication healing tail loss, committed-view membership, the restore-from-WAL proof), a mono kv sidebar top-right with just RUNS ON `data v4` · RIDE `conformance/schedule.test.ts` (in fero's CI · against data HEAD) · CONTRACT `SCHEDULE_VERSION 4`, and a single "fero →" link line. Beneath: the same muted `examples/` foot. 547px tall at 1440 — the mandated lead (4 lines) + the section chrome (padding + head, ~170px) is where the ≈380 target went; nothing animates. Sidebar stacks under the prose below 900px; kv labels stack over values below 480px.
**Smoke:** a — the 18/18 run is a canned replay (test names paraphrase the SCHEDULE clauses, timings random 0.1–0.5 ms, auto-replays every 11 s; gated on `varianthide` / off-screen / tab-hidden; reduced-motion renders the finished state). b — nothing; every word and name on the card is from fero's README / package.json and data's contract; the only link is the real GitHub URL.
**Wire for real:** a — run `conformance/schedule.test.ts` in-page through the node:test/node:assert shims and drive the strip from its reporter; link "read →" to wherever the essay lands. b — read `SCHEDULE_VERSION` off `contract/index.ts` (via `exportContract()`) instead of hard-coding the 4; the RIDE row could show fero's last contract-ride result if fero publishes a badge.
**Cut (b, per the pick):** the verbatim substrate import block (`../../data/api/index.ts` is a pre-publish relative path), the seven-name chips, the REC record shape, the drawn seam + its loop / counters (the wire is section 06 now), and the essay card. Earlier cuts stand: the old crossfilter / chat / to-do cards (examples/ retired at the v4 root — the foot says so), all `npm install` / kB / dist / ×-figure copy.
**Shots:** `shots/gallery-b.png`, `-1280`, `-390` (a: `gallery-a*.png` from the earlier round); zero console errors on every run; no third-party requests (the GitHub link is a plain href).
