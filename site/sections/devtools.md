**b · the inline dock card (THE PICK) — REAL (2026-09-14; honesty-reviewed the same day).** Sec-head 04 + two columns; the card on the right is the inline inspector, a projection of the real engine. `sections/devtools.js` builds this section's OWN five-node graph on the page runtime (`api` from engine.js): `trades = $({ t1…t5 })` → `buys = trades.filter(side === 'buy')` → `largest = buys.za('qty', 2)`, `typical = buys.median('px')`, `volume = buys.sum('qty')`. A scripted writer issues REAL writes under `runtime().withOrigin(Symbol('writer'), …)` / `runtime().batch(fn, origin)` every 1.2–1.9 s — `trades.t3.qty.update(395)`, `trades.t1.px.update(188.1)`, `trades.set('t6', {…})` (add), `trades.t2.side.update('buy')`, `trades.get('t6').remove()`, two two-key batches — a ten-step cycle that returns to the start state (every step is a genuine change; the kernel drops no-op writes, so none would commit). Four steps run synchronously on load (the prime) so every node has settled at least once before first paint: no `—` anywhere.

**What is real, by tier.** *runtime*: the Tree's `op#id` and `h<height>` (from `import('data/devtools').graph(trades)`, filtered to this section's five node ids — names come from the section's own id→name map), `N rows` (`rowCount()`), the two scalar cells (`render(cell, text(typical|volume, fmt))` — real render-layer sinks, so the render layer, not the card, writes them), the header `5 nodes · 3 sinks · h ≤ 2` (graph length · live `effects` on the five nodes: `trades.sink` for the events pane + the two `text()` sinks · max height — the real panel adds no sinks, only `onCommit` hooks, so the count holds with it open), the `3 sinks` line, the DAG's nodes/edges/column labels (the same graph's `edges` list, one column per height), the inspect strip (graph info · parents · `[value]` / `currentOrder()` / key lists), the Events pane's `seq N` (page-wide runtime seq — other sections share the runtime, so the numbers skip), the printed delta (`trades.sink({ apply })` → `batch.rows`: `update t3 .qty 380 → 395`, `add t6 { … }`, `remove t6`, `batch update t1 .qty … · update t4 .qty …`, from the real RowDelta `prev`/`row`/`path`), the cascade `trades 1Δ → buys 1Δ → largest 4Δ → …` (CommitInfo.nodes in settle order; nodes the kernel did not list are appended grey as `0Δ`), `N seen`, the footer's seq · nodes settled · `origin writer` (`c.origin.description`). *measured*: every ms — per-node from the CommitInfo (the kernel's `performance.now()` around each settle), Σ ms as their sum, both in the tree and per event line and in the footer. A settle below the tab's clock quantum prints `<0.1 ms` (the quantum is measured once at load — a busy-wait bounded to five ticks or ~25 ms — not assumed; Chrome's non-isolated clock is 100 µs). A Σ whose every reading is sub-quantum prints the only honest bound, `<n×quantum` (`<0.4 ms` for four settles), never `<0.1 ms`. *build*: nothing in this section. *synthetic input*: the five starting rows and the write script (the writes themselves; every printed RESULT is the engine's).

**The control.** "open the real panel ▸" lazy-imports `data/devtools-panel` (nothing under `lib/devtools/panel/` is fetched until the click — checked via resource timing) and calls `mountPanel({ open: true })` — the REAL closed-shadow dock docks to the right edge, showing the WHOLE page runtime's graph (every section's nodes), its Tree/DAG and inspector; a second click (or the dock's own ✕ — a body MutationObserver keeps the button honest) calls `handle.close()`; a third re-opens the singleton. The status line under the button carries no digits.

**Prose fact.** The lead-para no longer says the panel "mounts on demand, never on its own": `package.json` maps `./devtools` to `devtools/entry.ts`, which docks the panel on import in a browser unless the URL says `?nopanel` (and statically imports `panel/`), so that claim was false for the shipped entry. The rewrite states the three real cases: `devtools/index.ts` alone (what this page imports) never loads the panel; the `data/devtools` entry docks it on import unless `?nopanel`; `devtools/panel/` mounts only through `mountPanel()`. (`facts.md` §Devtools still says "Mounts on demand (never auto)" — a shared file, left for its owner; the source disagrees.)

**Kernel note.** `runtime().onCommit` hooks make the kernel measure per-node ms for EVERY commit on the page runtime (hooks.size > 0), not just this section's — that is by design (and what the real panel does too); the card's hook is subscribed only while the card is shown, on screen (IntersectionObserver, 120 px) and the tab visible, and disposed otherwise, together with the writer (`variantshow` / `varianthide` / `visibilitychange`). ⏸ pauses the writer only (the hook stays, so an outside write still prints). While the hook is OFF, the `N rows` cells are not repainted (the scalar cells are render sinks and never lag); every path that turns the hook back on (`hookOn`) re-reads `rowCount()` for the collection cells, the graph and the inspect strip BEFORE the next commit, so a card scrolled back into view is never behind the engine. The Events pane, `N seen` and the footer describe commits the card SAW (the hook was live) — commits made while it was off are not back-filled, and are labelled that way.

**Still presentation-only.** The flash/ring animations on changed cells and DAG nodes and the event-row slide-in — presentation of real events, never a source of a number; they rest under `prefers-reduced-motion` (and are stripped on tab switch so a hidden pane never replays old flashes). The tree's ▾/· carets and the `→` arrows are typography. A tree row that did not settle in the latest commit keeps its previous (measured) ms in grey; only the rows of the latest commit are green.

**a · the old form (not picked, untouched)** — moved verbatim to `sections/devtools/smoke-a.js` (`mountSmokeA`): the slide-in fixed dock over the tiny in-memory model with canned timings; still gated on `variantshow`/`varianthide`/`visibilitychange`. It never runs in the picked path (the module is imported and only wires variant a's hidden button).

**Verified.** `node proto/site-v8/tools/audit.mjs http://localhost:4176/ | grep '\[devtools\]'` → 0 lines, no page/console errors; `tools/probe.mjs --dwell 2500 4111 4227 4400` → 59.9 fps at each (the page-wide meter, section in view, other sections' writers running). Adversarial scratch probes (`scratchpad/devtools-refute.mjs`, `devtools-stale.mjs`): after the prime every tree id/height/op equals the node's `[node].id/.height/.opName`, every `N rows` equals `rowCount()`, both scalar cells equal `[value]`, the sinks count equals the live `effects` on the five handles, DAG = 5 nodes / 4 edges, `lib/devtools/panel/` has zero resource entries; off-screen (and under a stubbed `document.visibilityState = 'hidden'`) the writer makes zero commits in 4 s and the runtime's hook set is empty; in view the writer commits under origin `writer`, the newest event line's seq / cascade `NΔ` / per-node ms / Σ equal the CommitInfo captured by an independent `onCommit` hook, and every cell equals the handle read; stubbing `performance.now` to a constant collapses every "measured" figure to `<quantum` (Σ to `<n×quantum`) — no digit survives that is not from the clock; ⏸ → zero writer commits in 4 s, then one manual `write()` commits exactly seq+1 with its delta printed and the volume cell following the engine; an outside `remove t6` while off-screen leaves the collection cells behind only until the card comes back, which re-syncs them before any commit; the panel button mounts `[data-v3-devtools]` and the second click removes it, with the sinks count unchanged while it is open. Shots re-taken after the fixes: `shots/devtools-b.png`, `-1280`, `-390` (sec.mjs).
