**§05 The contract — variant b (the pick) is REAL.** The same law · proof table as the UI round (sec-head 05 + lead-para, boxed rows: LAW column · PROOF column · ✓ column, foot row + SCHEDULE.md link, muted `conform(node)` foot), backed by two things that run in the visitor's tab against the page's single engine (`engine.js` → `api/index.js`, type-stripped, served from `./lib`):

**1 · The suite.** `await import('data/schedule-suite')` loads the ACTUAL `conformance/schedule.test.ts` (node:test/node:assert shimmed by `shim/`); `import('data/shim-test').tests` is the 18-entry array it registered. Each `await t.fn()` runs sequentially in a try/catch, timed with `performance.now()`, yielding a task between tests so results paint as they land (the yield is outside the timed span). `gen/proofs.json` (fetched) maps test name → clause; if that fetch fails the clause is read from the test's own name (`clause 10c: …` → 10, the `SCHEDULE_VERSION` assert → head) so the rows can never sit at `·` while the foot reports a pass. Per clause row: the ✓ (pos) / ✗ (warn) is the real pass/fail of that clause's tests (`·` while pending; the row's `title` lists the test names with their ms); "N tests" = the build-tier count from proofs.json (`data-attested="build"`) — or, without proofs.json, the count of registered tests for that clause (`runtime`) — and after every run it is cross-checked against the tests that actually ran for that clause: on any drift the run's count replaces it (`runtime`). "M ms" = the sum of those tests' measured durations (`measured`). The head ✓ is the `SCHEDULE_VERSION` assert (test 0). The foot prints `tests.length` tests · passed · failed (all `runtime`, from the run) · total ms (`measured`, "this machine" — the sum of the per-test spans, not wall-clock). `window.__proofs = { pass, fail, total, failed }` is set at the end of every run — `audit.mjs --proofs 18` is satisfied. The run starts on load (after the first frame paints) and again on the "run the suite in this tab ▸" / "run again ▸" `.stream-btn`. Every test constructs its own `new Runtime()` (test 0 constructs none and writes nothing), so the suite never commits on the page runtime — verified by attribution, not by watching seq (see below).

**2 · The proof column.** Each clause's `<pre>` is a literal program (≤ 6 code lines): the JS reads the `<pre>`'s own text, renders it line by line (regex-highlighted), turns every bare `// →` line into an output slot, and executes THAT text through an `AsyncFunction` with the engine's real names in scope — `$ batch value node runtime ingest fromAsync handleFor` from `api/index.js` — plus two helpers: `print(text)` fills the next `// →` slot (`attest(…, 'runtime')`), `show(delta)` formats a RowDelta as `op key .path prev → next` (leaf-at-path). At the start of every run each slot is reset to an unattested bare `// →` first, so a line from a previous run can never stand as this run's output; a snippet that prints fewer lines than it has slots leaves the rest as plain `// →`. The snippets run on the PAGE runtime (so seqs printed are the page's real seqs and re-running advances `runtime().seq`), each inside a `scope()` from `lib/kernel/scope.js` (the served real module) that is disposed afterwards so the nodes/sinks they mint do not linger in `runtime().graph()`. A throwing snippet prints `// ✗ name: message` in warn (attested runtime) — never a fabricated line. What each prints: 01 the emitted delta + seq, then "nothing emitted, still 1 batch"; 02 `rowCount()` read mid-batch + sinks fired 0, then the sink at close; 03 a sink on the filter reading the settled `length()` value, then the `runtime().onCommit` settle order `source → filter → length`; 04 the caught `AggregateError` (1 sink failed) + rowCount before/after + sibling sink fired; 05 the two seqs (`a` then `b`, exactly next); 06 the origin-declaring sink skipped (0 applies), the other sink's `batch.origin === mine`; 07 init's snapshot size for a mid-batch `sink()` + the seq apply first fired at; 08 add+remove → 0 batches delivered, three updates → 1 delta with first prev/last value; 09 `fromAsync` default vs `{ coalesce: 'microtask' }` — 2 chunks → 2 commits vs 1 commit, same rowCount (the snippet waits while either `status()` is `'pending'`, so an `'error'` outcome prints instead of spinning; the async-iterator shape mirrors the suite's, since an async generator's extra hops defeat the same-tick premise); 10 the `deleted: true` update delta from a nested remove, the absent-target no-op (2 deltas for three writes), the vivified `{ depth: 3 }` under a null intermediate; 11 an ingested `'\x00key'` row with `NaN` / owned `undefined` read back from the source. No `print()` template carries a typed digit outside a `${…}` expression (checked mechanically).

**Tiers.** runtime: the lead-para's `SCHEDULE_VERSION` (`contract.SCHEDULE_VERSION`), every `// →` line, the foot's tests/passed/failed, the ✓/✗ states (and "N tests" only in the two fallback cases above). measured: per-clause ms, the foot's total ms. build: the per-clause "N tests" from gen/proofs.json. Literal typography (`data-literal`): the clause numerals 01–11. The gists for clauses 10/11 dropped their "(SCHEDULE_VERSION 2, fero W3)" / "(SCHEDULE_VERSION 3, W15)" parentheticals rather than carry typed digits.

**Synthetic input / presentation-only.** The snippets' rows (`AAPL 500`, `{ qty: 50 }`, `peer-A`, `'\x00key'`…) are the writes — synthetic input; every printed result is engine output. Presentation-only: the ✓ colour transition (180 ms, off under reduced-motion), the `// →` colour transition (220 ms, off under reduced-motion) and the between-test task yield that lets rows light up one by one (skipped under reduced-motion). Nothing animates continuously; no rAF subscriber, no timers at rest (the only timers are the two one-shot on-load kicks, the between-test yields and clause 9's own `setTimeout` polls while a run is in progress). Variant a (not picked) is untouched and still canned; its `TESTS` table is dead code on b's path.

**Smoke removed.** The canned per-test ms table, the guaranteed-pass stagger (`.law.proven`), the hand-written `// →` illustrations, and the typed "18 tests" / "SCHEDULE_VERSION 4" are gone from b's code path.

**Verified (builder + adversarial review, 2026-09-14).** `scratchpad/contract-drive.mjs` (builder) and `scratchpad/contract-refute.mjs` (review, 42 checks): on-load 18/18, every row `pass ✓`, every `// →` slot filled + `data-attested="runtime"` + no ✗, foot tiers runtime,runtime,runtime,measured, per-clause N = gen/proofs.json = tests that actually ran; **`performance.now` stubbed to a constant → every "measured" ms reads 0.00 and the foot 0.0, restored → > 0 again** (the ms are the performance API, nothing else); **one real test sabotaged to throw → its clause row ✗ + `fail`, foot 17 passed · 1 failed, `__proofs.fail` 1, the other ten rows still ✓; restored → all ✓**; **the DISPLAYED code of every row re-executed independently (same helpers, own scope) prints identical lines, seq-normalised** — the proof column is the program it shows; graph node count stable across repeated runs (20 → 20 → 20, scopes torn down); **the suite run alone on the page runtime: 6 commits during it touched only pre-existing nodes (other sections' writers), 0 foreign** — the suite never commits on the page runtime; clause 1's printed seq lies strictly between the seq before and after the run, clause 5's b-commit is a-commit + 1, "seq still" equals the delivered batch seq; `setTimeout`/`setInterval`/`requestAnimationFrame` instrumented from `addInitScript` — 0 calls from contract.js at rest over 2 s and 0 while variant b is hidden; the button advances `runtime().seq`; no unattested digits in variant b; zero console/page errors; reduced-motion context: 18/18 + every row pass; MutationObserver on a proof `<pre>`: each slot passes through unattested `// →` before this run's line lands; `gen/proofs.json` aborted via `page.route`: every row still ✓ from the test names, N attested runtime, no unattested digits. `audit.mjs --proofs 18`: 0 `[contract]` lines, proofs happy. `probe.mjs --dwell 2500 5018 6500 7800`: 59.9 fps at all three (section top / middle / bottom). Shots (`shots/real-contract-b{,-1280,-390}.png`) looked at: the table grammar, ✓ column, foot and stacking below 900px survive; proof lines wrap with a hanging indent so the whole literal program is readable at every width (390 clips of rows 1/9 and the foot read cleanly).
