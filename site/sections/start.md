**b · twelve lines (THE PICK) — REAL.** The panel's twelve lines EXECUTE against the page's engine (`import { api } from '../engine.js'` = `api/index.ts`, type-stripped, the page's one runtime): `$({ t1, t2, t3 })` → `trades.filter(t => t.side === 'buy')` → `buys.za('qty', 2)` → `buys.sum('qty')` → `trades.sink({ apply })` (captures its CommitBatch) → `render(ol, list(largest, t => el('li', null, el('b', null, t.sym), t.qty)))` into the small live `<ol>` under the panel → `batch(() => trades.t3.qty.update(380))`. The objects are created once when the panel scrolls in (IntersectionObserver, threshold 0.15) and kept alive; every `// →` cell is empty in the markup and filled by `attest()` the moment its line runs. `↻ replay` (and an idle loop every 8 s while the panel is on screen, the variant shown and the tab visible) re-reads the live views and lands the write again with the alternate value — 380 ↔ 500 (read off `trades.t3.qty[value]`) — and line 12's source text flips to the literal that actually ran.
**Runtime-attested:** `3 rows` = `trades.rowCount()` · `2 rows` = `buys.rowCount()` · `t3 · t1` = `largest[node].currentOrder()` (the `node` symbol is a public export; `keys()` is unordered by design and `snapshot()` drops keys, so the window's key order comes from the kernel node) · `700` / `580` = `volume[value]` · `update t3 .qty 500 → 380` = the sink's real batch (`rows[0].op / key / path`, `prev`/`row` read at `path`) · `1 commit · 1 delta` = `runtime().onCommit` calls during the batch and the source node's `deltas` in that CommitInfo (the hook is installed just before the write and disposed right after; note the runtime is page-wide, so other sections' hooks may keep the kernel's per-node timing on regardless — `commits` counts only calls during this batch) · the `<ol>`'s `<li>`s are the engine's render output (the host is stamped `runtime` at render time).
**Measured (this tab):** `2 <li>` = `ol.querySelectorAll('li').length` after render · `1 text · 0 created` = a MutationObserver on the `<ol>` (`subtree + childList + characterData`), drained with `takeRecords()` inside the commit hook — one `characterData` record, zero `addedNodes` — i.e. the list sink patched the qty text node in place and created nothing. **Build-tier:** nothing in this section.
**Order and pace:** the four outputs of the write are captured IN FIRE ORDER — the line-09 sink (effect phase) → the DOM patch (largest's list sink, measured) → the sum's settled value → the commit hook — and shown in that order. The definitions print at reading pace (~110 ms a line), the write lands after a ~650 ms beat; this stagger is presentation only (no number comes from a timer). Reduced motion: everything prints at once, no idle loop. Synthetic input: the three seed rows and the alternating 380/500 write — they are the writes; every printed result comes from the engine or the DOM.
**Deviations from the approved text:** line 10 says `render(ol, …)` instead of `render(document.body, …)` because it really renders into the `<ol>` shown under the panel ("ol · live"); line 09's `console.log(b)` does run (one batch object per write) and the panel prints the same batch. `.src` cells are `data-literal` (program text; the audit was flagging `qty: 200` etc.). The smoke's `FINAL`/`INIT`/`STEPS` tables, `data-init` attributes and the pre-baked outputs are deleted, not hidden. Variant a is untouched.
**Verified:** `tools/audit.mjs` → 0 `[start]` lines (the audit runs at scrollY 0, so a scratch driver — scratchpad `drive-start.mjs` — also scrolls the panel in, checks all seven cells + tiers + the `<ol>`, clicks `↻ replay`, asserts `runtime().seq` advanced (page-wide seq — other sections commit on the same runtime; start itself contributes exactly one commit per replay) and the cells flipped (`update t3 .qty 380 → 500`, `700`, `1 text · 0 created`, `1 commit · 1 delta`, line 12 → `update(500)`), replicates the audit's digit walk on `#start` in the exercised state, and confirms the idle loop stops off-screen: 25/25, zero console/page errors). `tools/probe.mjs --dwell 2500 3450 3677` → 59.9 fps at both offsets. Shots: `shots/start-b.png`, `-1280`, `-390` (sec.mjs, ~600 ms after scroll-in: the five reads printed, the write about to land) and `start-b-final-1440/1280/390.png` (after the write) — the panel, the head, the cta and the new one-line `<ol>` foot survive at all three widths; at 390 the panel scrolls horizontally as before.
**Reviewed (adversarial, 2026-09-14):** scratchpad `refute-start.mjs` (33/33) + `reduced-start.mjs`: the section's own source was located on the shared runtime via the CommitInfo its `onCommit` hook receives (look-alike 3-row `trades` sources exist in other sections — a naive registry search finds the wrong one), then driven from OUTSIDE with `handleFor` (`t1.qty := 900`): the live `<ol>` re-ordered at once and the next replay printed `t1 · t3` and `1,400` — the cells follow the engine, not a table. Stubbing `MutationObserver.prototype.takeRecords` to `[]` made line 10 read `0 text · 0 created` (it is the measurement); freezing `performance.now` changed nothing on the panel (no cell depends on the clock); starving `runtime().onCommit` left lines 08/10/12 without their post-write outputs (no constant fallback). `onCommit` is installed exactly once per write from start.js and disposed. The writer is silent off-screen, under `document.hidden` and while variant a is shown (0 writes over 10.5 s each), and fires once per ~8.4 s on screen; reduced motion prints all seven cells within 120 ms and never loops. Zero console/page errors on a clean run; 59.9 fps at scrollY 3562/3762; the audit's digit walk on `#start` is clean in the exercised state; shots at 1440/1280/390 (mid-run and final) preserve the look. Nothing in the section's code was changed by the review.
